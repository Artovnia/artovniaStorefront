export enum MessageSender {
    ADMIN = 'admin',
    SELLER = 'seller',
    USER = 'user',
  }
    
  export enum MessageThreadTypeEnum {
    ORDER = 'order',
    INQUIRY = 'inquiry',
    SUPPORT = 'support',
    GENERAL = 'general',
  }